CC = gcc
CFLAGS = -Wall -O2 -g -I.
LDFLAGS = -lm
PREFIX = {PREFIX}

jobqueue:	jobqueue.c version.h
	$(CC) $(CFLAGS) $(LDFLAGS) -o $@ $<

install:	jobqueue
	install jobqueue "$(PREFIX)/bin/"

check:	
	./selftest.sh

clean:	
	rm -f jobqueue
